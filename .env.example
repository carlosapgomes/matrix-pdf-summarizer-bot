# Matrix credentials
MATRIX_HOMESERVER=https://matrix.example.org
MATRIX_USER=@pdfbot:example.org
MATRIX_PASSWORD=super_secret_password
MATRIX_ROOM_ID=!yourroomid:example.org

# OpenAI (or compatible API) credentials
OPENAI_API_KEY=your_openai_api_key_here

# LLM Configuration
LLM_MODEL=gpt-5-mini
# LLM_BASE_URL=https://api.openai.com/v1  # Optional: for OpenAI-compatible APIs (Ollama, LM Studio, Azure OpenAI, etc.)
# LLM_TEMPERATURE=0.7  # Optional: defaults to model default
# LLM_MAX_TOKENS=  # Optional: defaults to model default

# Optional local session file
SESSION_FILE=session.json

# AI Prompt file for PDF summarization
PROMPT_FILE=prompts/medical_triage.txt

# Concurrent Processing Configuration
MAX_CONCURRENT_WORKERS=3  # Number of thread pool workers for PDF processing (default: 3)



